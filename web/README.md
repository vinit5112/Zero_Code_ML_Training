# Web UI (Next.js)

This folder is the dashboard frontend. **Run it together with the API from the repository root** — see the main [README](../README.md):

```bash
cd ..
npm start
```

For frontend-only development (API already running on port 8000):

```bash
npm install
npm run dev
```
